/**
 * Catch all route is dynamic route which catch all the route after a point and return remaning thing as array to handler
 * api/auth/[...nextAuth] this is a dynamic route which will catch 
 * api/auth/google/callback and return ["google","callback"] to handler
 * 
 * 
 */